<div class="container">
  <div class="container-inner inner-default clearfix">
    <% @items.each_with_index do |item, rank| %>
      <div class="item-box">
        <div class="image">
          <div class="img-wrap">
            <% if item.large_image.present? %>
              <%= image_tag(item.large_image , alt: item.title) %>
            <% else %>
              <%= image_tag("no-image.png" , alt: item.title) %>
            <% end %>
          </div>
        </div>
        <div class="detail">
          <p class="title">
            <%= rank + 1 %>位
            <%= item.title %>
          </p>
        </div>
      </div>
    <% end %>
  </div>
</div>
