<% content_for(:head) do %>
  <title>[<%= @user.name %>] Profile - monolist</title>
  <meta name="description" content="#{@user.name}さんのユーザーページです。">
<% end %>

<%= render 'profile' , user: @user %>


<%= render 'items/list' , items: @items %>